<aside class="inline-cta">
  <span class="inline-cta__copy">
    Enjoying this? Get the next essay in your inbox.
  </span>
  <a href="/newsletter" class="inline-cta__link">subscribe →</a>
</aside>

<style>
  .inline-cta {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 0.25rem 0.75rem;
    border: 1.5px dashed var(--muted);
    background: var(--card);
    padding: 0.75rem 1rem;
    margin: 2.5rem 0;
    font-size: 0.9rem;
  }
  .inline-cta__copy {
    color: var(--body-soft);
    font-family: ui-monospace, 'SF Mono', Menlo, Consolas, monospace;
    letter-spacing: 0.01em;
  }
  .inline-cta__link {
    font-weight: 600;
    color: var(--ink);
    white-space: nowrap;
    border-bottom: 2px solid var(--pink);
    padding-bottom: 1px;
    transition: background 160ms ease-out;
  }
  .inline-cta__link:hover {
    background: var(--pink);
  }
</style>
