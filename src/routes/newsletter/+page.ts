export const prerender = true;

export const load = () => ({
  meta: {
    ogTitle: 'Subscribe — homolova.sk',
    ogDescription: "Subscribe to Hi! It's Ada (essays + data) and The Pond (data training).",
    ogType: 'website' as const,
    canonical: '/newsletter'
  }
});
